[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
markers = [
    "integration: PostgreSQL integration tests (require TEST_DATABASE_URL)",
]
testpaths = ["tests"]
